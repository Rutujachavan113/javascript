//1. Continuous / Packed Array-->A packed array means the array has values at every index without gaps.

const arr = [10, 20, 30, 40];

console.log(arr);
// 10 → 20 → 30 → 40

// 2. Holey Array-->A holey array has an empty/missing index.
const arr2= [10, , 30, 40];

console.log(arr);
// 10 → empty → 30 → 40

