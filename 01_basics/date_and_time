const mydate=new Date()
console.log(mydate);

// convert to string
console.log(mydate.toString());
console.log(mydate.toDateString());//date and day
console.log(mydate.toLocaleDateString());//only date
console.log(typeof mydate);//object

let createDate=new Date(2026,0,25)//months stryt from zero
console.log(createDate.toDateString());

// +++++++timestamp+++++++++
let timestamp=Date.now()  //present time in miliseconds
console.log(timestamp);
console.log(createDate.getTime());//time of that date in miliseconds

// to custamize the date and time

let newDate=new Date();

console.log(newDate.toLocaleDateString('default',{
    weekday:'long',
    hour:"numeric"

}))







